folder images
db.js configurable json file
files which are in src and should be in target but are not
  deleteAllTargetFilesIn(_.without(target,src))
update files with error
Add config for supported extentions

file doc
  from: path_to_f1
  to: path_to_f2
  at: {c1, c2, c3}

when copying f1 to t1
  check if

